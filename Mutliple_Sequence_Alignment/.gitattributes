# Normalize line endings; keep shell scripts LF and batch files CRLF.
* text=auto
*.sh   text eol=lf
*.bat  text eol=crlf

# Treat generated binaries as binary (no diff/merge, no EOL conversion).
*.png  binary
*.jpg  binary
*.pdf  binary
*.pdb  binary
